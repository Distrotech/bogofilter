#! /bin/sh

. ${srcdir:=.}/t.frame

set -e

pwd
find . -name "t.lexer*" | xargs ls -l

unset LANG
unset LC_ALL
unset LC_COLLATE

cat inputs/t.lexer.inp | ../bogolexer -p | sort -u > ${TMPDIR}/t.lexer.out

cmp outputs/t.lexer.out ${TMPDIR}/t.lexer.out
