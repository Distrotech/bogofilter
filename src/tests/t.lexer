#! /bin/sh

. ${srcdir:=.}/t.frame

set -e

LANG=
unset LANG
LC_ALL=
unset LC_ALL
LC_COLLATE=
unset LC_COLLATE

cat ${srcdir}/inputs/t.lexer.inp | ${relpath}/bogolexer -p -C \
| sort | uniq > ${TMPDIR}/t.lexer.out

cmp ${srcdir}/outputs/t.lexer.out ${TMPDIR}/t.lexer.out
