#! /bin/sh

. ${srcdir:=.}/t.frame

set -e

unset LANG
unset LC_ALL
unset LC_COLLATE

cat ${srcdir}/inputs/t.lexer.inp | ../bogolexer -p | sort -u > ${TMPDIR}/t.lexer.out

cmp ${srcdir}/outputs/t.lexer.out ${TMPDIR}/t.lexer.out
