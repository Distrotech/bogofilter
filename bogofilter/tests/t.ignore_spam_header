#! /bin/sh

. ${srcdir=.}/t.frame

# test description here
#
# test below
# remember to use ${srcdir}

shn=`./spam_header_name`
hdr="From somewhere@example.com Fri Nov 22 11:30:23 2002"
shn1="$shn: blah1"
shn2="$shn: blah2"

( echo $hdr ; echo $shn1 ; echo ; echo $shn2 ) >$TMPDIR/input
tail -2 $TMPDIR/input >$TMPDIR/want
$VAL ../bogofilter -p -e -C < $TMPDIR/input | sed -n '/^$/,$ p' > $TMPDIR/out

cmp $TMPDIR/out $TMPDIR/want
