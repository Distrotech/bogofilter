#! /bin/sh

. ${srcdir}/t.frame
# test description here
#
# test below
# remember to use ${srcdir}
shn=`./spam_header_name`
( echo "$shn: blah1" ; echo ; echo "$shn: blah2" ) >$TMPDIR/input
tail -2 $TMPDIR/input >$TMPDIR/want
../bogofilter -p -e -C <$TMPDIR/input | sed -n '/^$/,$ p' >$TMPDIR/out
cmp $TMPDIR/out $TMPDIR/want
