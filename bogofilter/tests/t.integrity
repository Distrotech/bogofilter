#! /bin/sh

. ${srcdir}/t.frame
# test description here
#
# test below
# remember to use ${srcdir}
( cd ${TMPDIR} && uudecode ../t.integrity.uu )
../bogofilter -e -p <${TMPDIR}/input >${TMPDIR}/output
head -1 ${TMPDIR}/output | cmp - ${TMPDIR}/input
